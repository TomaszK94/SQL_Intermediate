/* 
Multiple WHEN Clauses in a single Case Block:

SELECT 
    CASE
        WHEN contidion1 THEN column
        WHEN condition2 THEN column
        ELSE column
    END AS alias
FROM 
    table_name;
*/
